/* Defined to the string representation of GStreamer version */
#define GST_API_VERSION_S @GST_API_VERSION@

/* Define to 1 if you have the <X11/Xatom.h> header file. */
#define HAVE_X11_XATOM_H @HAVE_X11_XATOM@

/* Define to 1 if you have the <X11/XKBlib.h> header file. */
#define HAVE_X11_XKBLIB_H @HAVE_X11_XKBLIB@

/* Define to 1 if you have the <X11/Xlib.h> header file. */
#define HAVE_X11_XLIB_H @HAVE_X11_XLIB@

/* Define to 1 if you have the <X11/Xutil.h> header file. */
#define HAVE_X11_XUTIL_H @HAVE_X11_XUTIL@

/* Defined to 1 if the XKB extension exists. */
#define HAVE_XKBLIB @HAVE_XKBLIB@

/* Defined to 1 if the XRandR extension exists. */
#define HAVE_XRANDR @HAVE_XRANDR@

/* Name of package */
#define PACKAGE "gstmfx"

/* Define to the full name of this package. */
#define PACKAGE_NAME "gst_msdk"

/* Define to the full name and version of this package. */
#define PACKAGE_STRING "gst_msdk @GST_MFX_VERSION@"

/* Define to the one symbol short name of this package. */
#define PACKAGE_TARNAME "gstreamer-msdk"

/* Define to the home page for this package. */
#define PACKAGE_URL ""

/* Define to the version of this package. */
#define PACKAGE_VERSION "@GST_MFX_VERSION@"

/* Defined to 1 if mfxdecode is enabled */
#define MFX_DECODE @MFX_DECODE@

/* Defined to 1 if mfxsink is enabled */
#define MFX_SINK @MFX_SINK@

/* Defined to 1 if mfxsencode is enabled */
#define MFX_ENCODE @MFX_ENCODE@

/* Defined to 1 if mfxvpp is enabled */
#define MFX_VPP @MFX_VPP@

/* Defined to 1 if DRM is enabled */
#define USE_DRM 1

/* Defined to 1 if EGL is enabled */
#define USE_EGL @HAVE_EGL@

/* Defined to 1 if video encoders are used */
#define USE_ENCODERS @HAVE_ENCODERS@

/* Defined to the set of enabled OpenGL ES APIs */
#define USE_GLES_VERSION_MASK 13

/* Defined to 1 if GStreamer OpenGL helper libraries are available */
#define USE_GST_GL_HELPERS 0

/* Defined to 1 if WAYLAND is enabled */
#define USE_WAYLAND @HAVE_WAYLAND@

/* Defined to 1 if X11 is enabled */
#define USE_X11 @HAVE_X11@

/* Version number of package */
#define VERSION "@GST_MFX_VERSION@"
